<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:fitsSystemWindows">false</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--hud theme-->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@drawable/hud_shape</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 周围变暗 -->
    </style>

    <!--手势返回theme-->
    <style name="BackSwipeTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@style/ActivitySwipeBackAnimation</item>
    </style>
    <style name="ActivitySwipeBackAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_swipe_in</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_swipe_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_swipe_in</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_swipe_out</item>
    </style>

    <!--透明Activity theme-->
    <style name="AlphaTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- iOs action sheet theme-->
    <style name="ActionSheetTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/ActionSheetAnimation</item>
    </style>
    <style name="ActionSheetAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/action_sheet_in</item>
        <item name="android:activityOpenExitAnimation">@anim/action_sheet_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/action_sheet_in</item>
        <item name="android:activityCloseExitAnimation">@anim/action_sheet_out</item>
    </style>

</resources>
